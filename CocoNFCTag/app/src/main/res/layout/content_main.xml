<FrameLayout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="tricurve.listadapterdemo.MainActivity"
    tools:showIn="@layout/activity_main">


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:id="@+id/contentMain_tabMainLay">

        <HorizontalScrollView
            android:layout_width="wrap_content"
            android:scrollbars="none"
            android:id="@+id/contentMain_scrollView"
            android:layout_height="wrap_content">


        <android.support.design.widget.TabLayout
                android:id="@+id/contentMain_tab_layout"
                android:layout_width="match_parent"
                android:layout_height="50sp"
                app:tabMaxWidth="0dp"
                app:tabGravity="fill"
                app:tabMode="fixed"
                android:minHeight="?attr/actionBarSize"/>

        </HorizontalScrollView>

        <android.support.v4.view.ViewPager
            android:id="@+id/contentMain_tab_pager"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_below="@+id/contentMain_draw_line_image_view"
            android:layout_weight="1"/>
    </LinearLayout>

</FrameLayout>